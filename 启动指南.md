# RAG 评估平台 - 启动指南

## 📋 项目概览

本项目包含：

- **后端**: FastAPI (Python) - 提供 RAG 评估 API
- **前端**: Vue 3 + Element Plus - 现代化评估管理平台

---

## 🚀 快速启动

### 步骤 1: 安装前端依赖

```bash
cd frontend
npm install
```

> **注意**: 如果遇到网络问题，可以使用淘宝镜像：
>
> ```bash
> npm config set registry https://registry.npmmirror.com
> npm install
> ```

### 步骤 2: 启动后端服务

**方式 1: 直接运行**

```bash
# 在项目根目录
cd evaluation
python main.py
```

**方式 2: 使用 uvicorn**

```bash
cd evaluation
uvicorn main:app --reload --port 8000
```

后端将在 http://localhost:8000 启动

### 步骤 3: 启动前端开发服务器

```bash
# 在 frontend 目录
cd frontend
npm run dev
```

前端将在 http://localhost:3000 启动

---

## 🎯 访问平台

打开浏览器访问: **http://localhost:3000**

### 功能导航

1. **仪表盘** (`/dashboard`): 查看整体评估概况
2. **评估任务** (`/tasks`): 管理评估任务列表
3. **样本调试** (`/debug`): 单条样本实时评估
4. **版本对比** (`/compare`): 不同版本指标对比

---

## 📊 测试评估功能

### 方式 1: 通过前端界面

1. 访问 "样本调试" 页面
2. 填写表单：
   - **用户问题**: "谁是中国最受欢迎的歌手？"
   - **检索上下文**: "周杰伦是华语流行音乐歌手"
   - **模型回答**: "周杰伦"
   - **标准答案**: "周杰伦"
3. 点击 "执行评估" 按钮

### 方式 2: 直接调用 API

```bash
curl -X POST "http://localhost:8000/api/rag/evaluation" \
  -H "Content-Type: application/json" \
  -d '{
    "user_input": ["谁是中国最受欢迎的歌手？"],
    "response": ["周杰伦"],
    "retrieved_contexts": [["周杰伦是华语流行音乐歌手"]],
    "reference": ["周杰伦"]
  }'
```

---

## 🛠️ 常见问题

### Q1: 前端 TypeScript 报错

**A**: 这是正常的，运行 `npm install` 后会自动解决。

### Q2: 无法连接后端

**A**:

1. 确认后端已启动在 8000 端口
2. 检查 `.env` 文件配置
3. 查看浏览器开发者工具 Network 面板

### Q3: ECharts 图表不显示

**A**:

1. 确保有评估数据
2. 尝试刷新页面
3. 检查浏览器控制台错误

### Q4: 样式错误或组件不显示

**A**:

1. 清除浏览器缓存
2. 重启开发服务器
3. 检查 Element Plus 是否正确安装

---

## 📦 生产环境部署

### 构建前端

```bash
cd frontend
npm run build
```

构建产物在 `frontend/dist/` 目录

### 部署方式

**方式 1: Nginx 静态托管**

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://localhost:8000;
    }
}
```

**方式 2: Docker 部署**

```dockerfile
# 前端 Dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=0 /app/dist /usr/share/nginx/html
```

---

## 🎨 UI 预览

### 主界面特点

- **深蓝主题** (#1E40AF): 专业、科技感
- **卡片式布局**: 简洁、大气
- **响应式设计**: 适配大屏显示
- **渐变图标**: 美观、现代

### 核心页面

1. **仪表盘**: 统计卡片 + 最近任务 + 快捷入口
2. **任务列表**: 筛选搜索 + 状态管理 + 指标预览
3. **任务详情**: 雷达图 + 柱状图 + 样本列表
4. **样本调试**: 表单输入 + 实时评估 + 进度条可视化
5. **版本对比**: 双版本选择 + 对比表格 + 雷达图

---

## 📖 API 文档

访问 http://localhost:8000/docs 查看 Swagger UI 自动生成的 API 文档。

---

## 💡 开发建议

1. **代码风格**: 遵循 TypeScript + ESLint + Prettier
2. **组件化**: 优先使用 Composition API
3. **状态管理**: 复杂状态使用 Pinia store
4. **性能优化**: 使用 `v-if` 而非 `v-show` 减少渲染
5. **类型安全**: 充分利用 TypeScript 类型检查

---

## 📞 技术支持

如有问题，请检查：

1. Node.js 版本 >= 16
2. Python 版本 >= 3.8
3. 依赖是否完整安装
4. 端口 3000 和 8000 是否被占用

---

**祝你使用愉快！🎉**
